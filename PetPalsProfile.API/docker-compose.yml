version: '3.4'

services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_DB: "PetPalsProfile"
      POSTGRES_USER: "pguser"
      POSTGRES_PASSWORD: "pgpassword"
    ports:
      - "5432:5432"